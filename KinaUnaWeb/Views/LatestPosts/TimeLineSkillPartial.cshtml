@using KinaUnaWeb.Services
@using KinaUna.Data
@model Skill
@inject ILocaleManager LocaleManager
@{
    int languageId = LocaleManager.GetLanguageId(Context.Request);
}
<a class="leavePage" asp-controller="Skills" asp-action="Index">
    <div class="bg-primary timeline-card">
        <div class="timeline-card-body bg-skill">
            <div class="row">
                <div class="col-auto">
                    <span class="latest-posts-icon"><i class="material-icons">school</i></span>
                </div>
                <div class="col">
                    <div class="latest-posts-title">
                        <strong>
                            @Model.Name
                        </strong>
                    </div>
                    <div class="latest-posts-text text-white-50">
                        @if (Model.SkillFirstObservation.HasValue)
                        {
                            <div>@await LocaleManager.GetTranslation("Date:", PageNames.Skills, languageId) @Model.SkillFirstObservation.Value.ToString("dd-MMM-yyyy")</div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</a>